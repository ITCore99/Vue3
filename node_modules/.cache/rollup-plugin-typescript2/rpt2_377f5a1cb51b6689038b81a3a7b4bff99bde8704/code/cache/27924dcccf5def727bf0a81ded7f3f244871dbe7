{"code":"// 对元素设置属性\r\nexport const patchAttr = (el, key, value) => {\r\n    if (value === null) {\r\n        el.removeAttribute(key);\r\n    }\r\n    else {\r\n        el.setAttribute(key, value);\r\n    }\r\n};\r\n","references":[]}
